{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UsuarioEstablecimiento</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Idestablecimiento</th>
                <td>{{ entity.idEstablecimiento }}</td>
            </tr>
            <tr>
                <th>Idusuario</th>
                <td>{{ entity.idUsuario }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('usuarioestablecimiento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('usuarioestablecimiento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
